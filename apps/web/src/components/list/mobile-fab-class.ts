/** Shared styling for a list page's mobile "thumb-zone" floating action
 * button — a second trigger for the SAME primary Add action the toolbar
 * already renders (top-right, desktop-reachable), fixed bottom-right and
 * clear of the bottom tab bar, hidden at md+ where the toolbar button already
 * sits in comfortable reach. */
export const MOBILE_FAB_CLASS =
	"fixed right-4 bottom-20 z-30 size-12 rounded-full shadow-lg md:hidden";
