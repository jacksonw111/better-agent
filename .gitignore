# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
*.tsbuildinfo
.tsbuild

# Generated files
# NOTE: web/admin/authz commit their routeTree.gen.ts — CI type-checks them with
# tsc and there's no build step before it to generate the tree.
apps/authz/app/dist

# Environment variables
.env
.env*.local

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Turbo
.turbo
.nx

# Better-T-Stack
.alchemy

# Testing
coverage
.nyc_output

# Misc
*.tgz
.cache
tmp
temp

# k3s prod secrets: only the sops-ENCRYPTED file may be committed. The
# plaintext working copy must never land in git (encrypt in place instead).
deploy/secrets/*.plaintext.yaml

# docker-compose secrets (only .env.example is committed)
deploy/compose/.env
.pi-*
.superpowers
 